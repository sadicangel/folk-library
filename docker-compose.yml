version: '3.4'

services:
  folklibrary.webapi:
    container_name: folklibrary_webapi
    image: ${DOCKER_REGISTRY-}folklibrarywebapi
    build:
      context: .
      dockerfile: FolkLibrary.WebApi/Dockerfile
    volumes:
    - /d/music/folk:/app/data
    - ./rabbitmq/definitions.json:/app/rabbitmq_definitions.json
    depends_on:
    - postgres
    - mongodb
    - rabbitmq
  folklibrary.functions:
    container_name: folklibrary_functions
    image: ${DOCKER_REGISTRY-}folklibraryfunctions
    build:
      context: .
      dockerfile: FolkLibrary.Functions/Dockerfile
    depends_on:
    - postgres
    - mongodb
    - rabbitmq
  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:3-management
    ports:
    - 15672:15672
    volumes:
    - ./rabbitmq/definitions.json:/etc/rabbitmq/definitions.json
    environment:
    - RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=-rabbitmq_management load_definitions "/etc/rabbitmq/definitions.json"
  postgres:
    container_name: postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: folklibrary
  postgres.admin:
    container_name: postgres_admin
    image: dbeaver/cloudbeaver
    ports:
    - 8978:8978
    depends_on:
    - postgres
  mongodb:
    container_name: mongodb
    image: mongo
  mongodb.admin:
    container_name: mongodb_admin
    image: mongo-express
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_SERVER: mongodb
    depends_on:
    - mongodb

